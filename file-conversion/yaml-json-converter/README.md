# YAML-JSON Converter 🔄
Bidirectional conversion between YAML and JSON.
## Quick Start
```bash
python main.py convert data.json    # auto-detects and converts to YAML
python main.py convert config.yaml  # auto-detects and converts to JSON
python -m pytest tests/ -v
```
