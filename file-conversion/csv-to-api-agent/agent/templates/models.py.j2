from database import db

class Item(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    {% for col in columns %}
    {{ col.name }} = db.Column(db.{{ col.sql_type }})
    {% endfor %}

    def to_dict(self):
        return {
            'id': self.id,
            {% for col in columns %}
            '{{ col.name }}': self.{{ col.name }},
            {% endfor %}
        }
