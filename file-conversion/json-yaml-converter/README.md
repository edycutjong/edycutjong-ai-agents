# JSON/YAML/TOML Converter 🔄

Convert between JSON, YAML, and TOML formats. Zero external dependencies — custom serializers.

## Quick Start

```bash
pip install -r requirements.txt
python main.py convert data.json --to yaml
python main.py validate data.json
python main.py format data.json --compact
python main.py detect data.yml
python -m pytest tests/ -v
```
