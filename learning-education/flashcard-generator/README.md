# Flashcard Generator 📇
Create study flashcards from topics, terms, or text. Export to Anki.
## Quick Start
```bash
python main.py generate python --count 10
python main.py generate javascript --anki
python -m pytest tests/ -v
```
