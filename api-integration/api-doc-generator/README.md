# API Doc Generator 📝
Parse Flask/Express/FastAPI code and generate OpenAPI specs or Markdown docs.
## Quick Start
```bash
python main.py generate app.py --format openapi
python main.py generate server.js --framework express
python -m pytest tests/ -v
```
