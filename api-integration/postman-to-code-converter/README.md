# Postman to Code Converter 📮→💻

Convert Postman collections to Python, curl, JavaScript, and Go code.

## Quick Start
```bash
pip install -r requirements.txt
python main.py convert collection.json --language python
python main.py convert collection.json -l curl
python main.py languages
python -m pytest tests/ -v
```
