# GraphQL Schema Analyzer 📊
Analyze GraphQL SDL schemas: parse types, detect unused types, find complexity issues.
## Quick Start
```bash
python main.py analyze schema.graphql
python -m pytest tests/ -v
```
