# Slug Generator 🔗
Convert text to URL-friendly slugs with Unicode support and batch deduplication.
## Quick Start
```bash
python main.py generate "Hello World!"
python main.py generate "über cool" --sep _
python -m pytest tests/ -v
```
