# README Generator 📝
Auto-generate project READMEs with badges, features, and quick start sections.
## Quick Start
```bash
python main.py generate --dir ./my-project
python main.py generate --name "My App" --template api
python -m pytest tests/ -v
```
