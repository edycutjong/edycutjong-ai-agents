# Contract Analyzer 📄

Analyze contracts to extract clauses, detect risks, and generate recommendations. Pure Python, no AI needed.

## Quick Start
```bash
pip install -r requirements.txt
python main.py analyze contract.txt
python main.py analyze contract.txt --json
python -m pytest tests/ -v
```
