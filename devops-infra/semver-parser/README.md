# SemVer Parser 📦
Parse, compare, and validate semantic versions with range support.
## Quick Start
```bash
python main.py parse "1.2.3-beta+build.1"
python main.py bump "1.2.3" --part minor
python -m pytest tests/ -v
```
