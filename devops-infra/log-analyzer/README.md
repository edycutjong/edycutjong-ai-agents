# Log Analyzer 📊
Parse, filter, and analyze log files with multi-format support.
## Quick Start
```bash
cat app.log | python main.py analyze -
python main.py analyze app.log --level error
python -m pytest tests/ -v
```
