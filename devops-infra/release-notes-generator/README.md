# Release Notes Generator 📋
Generate changelogs from conventional commit messages.
## Quick Start
```bash
git log --oneline | python main.py generate - --version v1.0.0
python -m pytest tests/ -v
```
