# Docker Compose Generator 🐳
Generate docker-compose.yml from pre-built service templates.
## Quick Start
```bash
python main.py generate postgres redis nginx
python main.py list
python -m pytest tests/ -v
```
