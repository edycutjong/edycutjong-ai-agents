# Config File Merger 🔀
Deep merge multiple config files with conflict detection and resolution strategies.
## Quick Start
```bash
python main.py merge base.json override.json --report
python -m pytest tests/ -v
```
