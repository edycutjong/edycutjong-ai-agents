# Dockerfile Generator 🐳
Generate optimized Dockerfiles for Python, Node, Go, Rust, Java, and static sites.
## Quick Start
```bash
python main.py generate python --port 8080
python -m pytest tests/ -v
```
