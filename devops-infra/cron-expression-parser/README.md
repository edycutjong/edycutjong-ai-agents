# Cron Expression Parser ⏰
Parse and explain cron expressions with human-readable descriptions.
## Quick Start
```bash
python main.py parse "*/5 * * * *"
python main.py parse "@daily"
python -m pytest tests/ -v
```
