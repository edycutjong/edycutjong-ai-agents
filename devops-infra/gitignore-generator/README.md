# Gitignore Generator 🚫
Generate .gitignore files from templates for Python, Node, Java, Go, Rust, and more.
## Quick Start
```bash
python main.py generate python node general
python main.py list
python -m pytest tests/ -v
```
