# Changelog Generator 📝
Generate changelogs from conventional commit messages with version bump suggestions.
## Quick Start
```bash
echo "feat: add login\nfix: resolve bug" | python main.py generate - --version 1.0.0
python -m pytest tests/ -v
```
