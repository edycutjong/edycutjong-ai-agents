# IP Geolocation Lookup 🌍
Look up IP addresses with validation, private range detection, and mock geolocation.
## Quick Start
```bash
python main.py lookup 8.8.8.8
python main.py lookup 192.168.1.1
python -m pytest tests/ -v
```
