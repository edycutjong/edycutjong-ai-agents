# Accessibility Auditor ♿
Check HTML for WCAG compliance: images, links, headings, forms, and more.
## Quick Start
```bash
python main.py audit index.html
cat page.html | python main.py audit -
python -m pytest tests/ -v
```
